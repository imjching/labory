<div class="subhead">
  <h2 class="subhead-heading">New lab</h2>
</div>

<%= form_for [:stafftools, @course, @lab] do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <%= render partial: 'stafftools/labs/lab_form_options', locals: { f: f, status: 'Create' } %>
<% end %>